name: Sync API every 1 min

on:
  schedule:
    - cron: "* * * * *"   # every minute
  workflow_dispatch:

jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Call API
        run: curl https://displayrates.devi-jewellers.com/api/rates/sync
